# ====== 环境变量配置示例 ======
# 复制为 .env 并填入真实值

# 阿里云百炼 API Key（必填）
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxx

# PostgreSQL 数据库（如果用容器内 PG，保持默认即可；如果用阿里云 RDS，填 RDS 地址）
DB_HOST=db
DB_PORT=5432
DB_NAME=yu_ai_agent
DB_USERNAME=my_user
DB_PASSWORD=your_password_here
